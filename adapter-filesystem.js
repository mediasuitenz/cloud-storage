module.exports = config => {

  // use config for setup here

  return {
    upload () {
      return new Promise((resolve, reject) => {

      })
    },
    download () {
      return new Promise((resolve, reject) => {

      })
    }
  }
}
